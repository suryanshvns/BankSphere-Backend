[project]
name = "banksphere-backend"
version = "1.0.0"
description = "Core banking API (FastAPI + PostgreSQL + Prisma)"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
